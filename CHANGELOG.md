# Changelog

本项目的重要版本变更记录如下。

> 2026-09-17 重建说明：`v1.0.0` 至 `v1.2.1` 的 GitHub Release 已按当前
> `main` 代码重新构建。五个版本使用同一份最新代码和相同功能，版本章节保留
> 各阶段的开发里程碑，便于追溯。

## [v1.2.1] - 2026-09-17

### 新增

- 新增 `settings.cfg` 独立支持与解析，明确区分操作设置、游戏画质和启动命令。
- 默认配置路径改为 `%USERPROFILE%\Saved Games\Respawn\Apex\local`，优先探测
  `settings.cfg`，并兼容同目录的 `videoconfig.txt`。
- 新增窗口四边与四角拖拽缩放命中区，无边框窗口可稳定调整尺寸。
- 新增长文本编辑区滚动测试与 `settings.cfg` 解析测试。

### 修复

- 重新选择 cfg 文件增加醒目按钮，并扩大标题栏图标命中区域。
- 修复上次选择目录失效时文件选择器无法打开的问题。
- 修复关闭应用时的额外等待，干净文档直接走原生关闭流程。
- 优化小字号字体渲染，正文统一使用 `Microsoft YaHei UI` 字体回退链。
- 文本编辑器在固定高度面板中保持可滚动。

### 调整

- 自动检测范围收窄：配置文件只扫描 Saved Games；游戏目录只保留 EA App。
- 移除 Steam、Documents、OneDrive 等自动探测路径，手动选择与 EA App
  安装目录检测保持可用。

## [v1.2.0] - 2026-09-16

### 新增

- 编辑器迁移到 Fluent UI 控件体系。
- 新增设置诊断、应用信息、更新检查与发行说明入口。
- 新增可手动触发的 Windows 构建与发布工作流。

### 修复

- 修复更新检查响应的异步等待与错误处理。
- 修复发布工作流中的权限、版本号和安装器处理问题。

## [v1.1.0] - 2026-09-07

### 新增

- 新增自动探测引擎 v2，支持 EA App、Steam、OneDrive 与自定义安装目录。
- 新增 `autoexec.cfg` 创建模板、多安装选择与安装路径记忆。
- 新增 Inno Setup 单文件安装器和便携版 ZIP。
- 新增酸性风格主题、无边框窗口、自绘标题栏与亮暗主题记忆。
- 更新 UI 设计规范、Windows Release 构建和安装说明。

### 修复

- 修复 Windows CI 路径分隔符、Inno Setup 参数和安装脚本兼容性问题。

## [v1.0.1] - 2026-09-06

### 修复

- 修复 CRLF、BOM、备份时间戳、坏字节保存和还原失败反馈等稳定性问题。
- 改进脏状态语义、备份排序和还原确认流程。
- 修复 Windows Release 创建所需的仓库权限配置。

## [v1.0.0] - 2026-09-06

### 新增

- 首个稳定版：支持 `videoconfig.txt` 与 `autoexec.cfg` 的表格和全文编辑。
- 新增 UTF-8 / GBK 编码探测、按原编码保存、坏字节保护与原子写入。
- 新增逐行实时 diff、内置中英文知识库、备份与一键还原。
- 新增 Windows 路径探测、手动文件选择、退出保护和完整中英文界面。
- 新增测试夹具、Windows CI 和 MIT License。
