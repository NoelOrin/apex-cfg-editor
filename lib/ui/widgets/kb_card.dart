import 'package:flutter/material.dart';

/// 知识卡占位：真实实现由后续任务填充。
/// 无参构造是任务 10 确定的接线，保持稳定。
class KbCard extends StatelessWidget {
  const KbCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
